#pragma once

namespace GOAT_ENGINE {
	//Camera
	const float MIN_DRAW_DISTANCE(0.1f);
	const float MAX_DRAW_DISTANCE(20.0f);
	const float CAM_DISTANCE(5.0f);
	const float CAM_FOV(90.0f);

	//Window
	const int WIN_WIDTH(800);
	const int WIN_HEIGHT(800);
}